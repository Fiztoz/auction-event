# frozen_string_literal: true

source 'https://rubygems.org'

ruby '~> 3.2'

# Web framework
gem 'sinatra', '~> 4.0'
gem 'sinatra-contrib', '~> 4.0'

# Database
gem 'mysql2', '~> 0.5'

# Background jobs / Event processing
gem 'bunny', '~> 2.20'  # RabbitMQ client

# JSON
gem 'json', '~> 2.7'

# Environment variables
gem 'dotenv', '~> 2.8'

# Web server
gem 'rackup'
gem 'puma', '~> 6.0'

group :development, :test do
  gem 'rake', '~> 13.0'
  gem 'minitest', '~> 5.0'
end
